"use strict";import SeriesRegistry from"../../Core/Series/SeriesRegistry.js";const{seriesTypes:{mapline:{prototype:{pointClass:MapLinePoint}}}}=SeriesRegistry;import U from"../../Core/Utilities.js";const{pick,isString,isNumber}=U;class FlowMapPoint extends MapLinePoint{isValid(){let valid=!!(this.options.to&&this.options.from);[this.options.to,this.options.from].forEach(function(toOrFrom){valid=!!(valid&&(toOrFrom&&(isString(toOrFrom)||isNumber(pick(toOrFrom[0],toOrFrom.lat))&&isNumber(pick(toOrFrom[1],toOrFrom.lon)))))});return valid}}export default FlowMapPoint;