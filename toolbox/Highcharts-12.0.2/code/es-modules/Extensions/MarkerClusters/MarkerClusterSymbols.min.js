"use strict";let symbols;function cluster(x,y,width,height){const w=width/2,h=height/2,outerWidth=1,space=1,inner=symbols.arc(x+w,y+h,w-space*4,h-space*4,{start:Math.PI*.5,end:Math.PI*2.5,open:false}),outer1=symbols.arc(x+w,y+h,w-space*3,h-space*3,{start:Math.PI*.5,end:Math.PI*2.5,innerR:w-outerWidth*2,open:false}),outer2=symbols.arc(x+w,y+h,w-space,h-space,{start:Math.PI*.5,end:Math.PI*2.5,innerR:w,open:false});return outer2.concat(outer1,inner)}function compose(SVGRendererClass){symbols=SVGRendererClass.prototype.symbols;symbols.cluster=cluster}const MarkerClusterSymbols={compose:compose};export default MarkerClusterSymbols;