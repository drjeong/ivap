"use strict";import SeriesRegistry from"../../../Core/Series/SeriesRegistry.js";const{ema:EMAIndicator}=SeriesRegistry.seriesTypes;import U from"../../../Core/Utilities.js";const{extend,merge,error}=U;class APOIndicator extends EMAIndicator{getValues(series,params){const periods=params.periods,index=params.index,APO=[],xData=[],yData=[];let oscillator,i;if(periods.length!==2||periods[1]<=periods[0]){error('Error: "APO requires two periods. Notice, first period '+'should be lower than the second one."');return}const SPE=super.getValues.call(this,series,{index:index,period:periods[0]});const LPE=super.getValues.call(this,series,{index:index,period:periods[1]});if(!SPE||!LPE){return}const periodsOffset=periods[1]-periods[0];for(i=0;i<LPE.yData.length;i++){oscillator=SPE.yData[i+periodsOffset]-LPE.yData[i];APO.push([LPE.xData[i],oscillator]);xData.push(LPE.xData[i]);yData.push(oscillator)}return{values:APO,xData:xData,yData:yData}}}APOIndicator.defaultOptions=merge(EMAIndicator.defaultOptions,{params:{period:void 0,periods:[10,20]}});extend(APOIndicator.prototype,{nameBase:"APO",nameComponents:["periods"]});SeriesRegistry.registerSeriesType("apo",APOIndicator);export default APOIndicator;"";