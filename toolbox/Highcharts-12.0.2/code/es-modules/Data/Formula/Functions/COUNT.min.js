"use strict";import FormulaProcessor from"../FormulaProcessor.js";function COUNT(args,table){const values=FormulaProcessor.getArgumentsValues(args,table);let count=0;for(let i=0,iEnd=values.length,value;i<iEnd;++i){value=values[i];switch(typeof value){case"number":if(!isNaN(value)){++count}break;case"object":count+=COUNT(value,table);break}}return count}FormulaProcessor.registerProcessorFunction("COUNT",COUNT);export default COUNT;