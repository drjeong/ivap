import*as dom from"../../dom/index.js";export const renderTitle=(instance,params)=>{const title=dom.getTitle();if(!title){return}dom.showWhenInnerHtmlPresent(title);dom.toggle(title,params.title||params.titleText,"block");if(params.title){dom.parseHtmlToContainer(params.title,title)}if(params.titleText){title.innerText=params.titleText}dom.applyCustomClass(title,params,"title")};